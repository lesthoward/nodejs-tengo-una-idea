doctype html
html(lang="en")
    head
        include partials/head
        link(rel="stylesheet", href="/css/blocks/header.css")
        link(rel="stylesheet", href="/css/login&register.css")
        meta(name="robots" content="noindex")
    body 
        include partials/header
        .container 
            form.user(action="/user/forget" method="POST") 
                .user__wrapper
                    h2.user__title Olvidé mi contraseña
                    if locals.authErrors.length
                        .messages
                            each error in locals.authErrors
                                p.messages__error=error
                    .user__field
                        label.user__label(for="email" autofocus) Correo:
                        input.user__input(type="email", name="email" id="email" autofocus)
                    .user__action
                        button.user__button.user__button--complete ¡Enviar solicitud! (confirmar conrreo) 
                        a.user__redirect.user__redirect--complete(href="/login") Ya recordé mi contraseña